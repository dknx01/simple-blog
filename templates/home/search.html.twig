{% extends 'layout/conatiner_fluid.html.twig' %}

{% block title %}{{ name }}{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <div class="card card">
        <div class="card-header">Suche</div>
        <div class="card-body">
            <form method="post">
                <div class="row card-text">
                    <div class="col col-md-10 col-sm-10">
                        <input type="text" value="{{ searchText }}" name="searchText" id="inputSearchText" class="form-control py-4" required autofocus>
                    </div>
                    <div class="col">
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('search') }}" >
                        <button class="btn btn-lg btn-partei" type="submit">
                            Suchen
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <div style="height: 30px"></div>
    {% if app.request.method == "POST" %}
    <div class="card card">
        <div class="card-header">Suchergebnisse</div>
        <div class="card-body">
            <div class="list-group">

                {% if result|length == 0 %}
                    <div class="alert alert-warning" role="alert">
                        Keine Ergebnisse gefunden
                    </div>
                {% else %}
                    {% for result in result %}
                        <a href="#{{ result.path|url_encode }}" class="list-group-item list-group-item-action">
                            {{ result.name }}
                    </a>
                    {% endfor %}
                {% endif %}
            </div>
        </div>
    </div>
    {% endif %}
</div>
{% endblock %}
