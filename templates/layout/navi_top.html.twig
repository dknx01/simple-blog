<nav class="sb-topnav navbar navbar-expand-md navbar-light bg-light">
    <a href="{{ path('home') }}"><div class="ribbon ribbon-top-left"><span>{{ 'version'|trans({}, 'menu') }}</span></div></a>
    <a class="navbar-brand" href="{{ path('home') }}"><img src="{{ asset('build/dp_ovtk_logo.png') }}" alt="{{ 'logo_alt_text'|trans({}, 'menu') }}"></a>
{#    <span class="navbar-brand mb-0 h1 custom-brand">{{ 'brand'|trans({}, 'menu') }}</span>#}
        <button id="main_menu" class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    {% if is_granted(constant('App\\Security\\Roles::ROLE_ADMIN')) %}
        <button class="btn btn-link btn-sm order-1 order-lg-0" id="sidebarToggle" href="#"><i class="fas fa-bars"></i></button>
    {% endif %}
    <div class="collapse navbar-collapse" style="background-color: #dee2e6 !important;" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto ml-md-0">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" id="stammtischDropdown" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">{{ 'stammtische'|trans({}, 'menu') }}</a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="stammtischDropdown">
                    <a class="dropdown-item" href="{{ path('list-stammtische', {'path': 'LV'}) }}">{{ 'lv_stammtisch'|trans({}, 'menu') }}</a>
                    <a class="dropdown-item" href="{{ path('list-stammtische', {'path': 'OV TK'}) }}">{{ 'ov_stammtisch'|trans({}, 'menu') }}</a>
            </div>
        </li>

        <li class="nav-item">
            <a class="nav-link-top nav-link" href="{{ path('documents') }}">{{ ( 'dokumente')|trans({}, 'menu') }} <span class="sr-only"></span></a>
        </li>
        <li class="nav-item">
            <a class="nav-link-top nav-link" href="{{ path('link-collection') }}">{{ ( 'linksammlung')|trans({}, 'menu') }} <span class="sr-only"></span></a>
        </li>
        <li class="nav-item">
            <a class="nav-link-top nav-link" href="{{ path('newbees') }}">{{ ('newbees')|trans({}, 'menu') }} <span class="sr-only"></span></a>
        </li>
        <li class="nav-item">
            <a class="nav-link-top nav-link" href="{{ path('wiki_index') }}">{{ ( 'wiki')|trans({}, 'menu') }} <span class="sr-only"></span></a>
        </li>
        <li class="nav-item">
            <a class="nav-link-top nav-link" href="{{ path('suggestion_index') }}">{{ ( 'suggestion')|trans({}, 'menu') }} <span class="sr-only"></span></a>
        </li>
    </ul>

        <!-- Navbar Search-->
        <form class="form-inline my-2 my-lg-0" method="post" action="{{ path('search') }}">
            <div class="input-group">
            <input class="form-control mr-sm-2" type="search" placeholder="suchen" aria-label="Suche" name="searchText" {% if searchText is defined %}value="{{ searchText }}" {% endif %}>
            <input type="hidden" name="_csrf_token" value="{{ csrf_token('search') }}" >
            <button class="btn btn-partei" type="submit"><i class="fas fa-search"></i></button>
            </div>
        </form>
        <!-- Navbar-->

    <!-- Navbar Admin-->
    <ul class="navbar-nav d-md-inline-block form-inline ml-auto mr-0 mr-md-0 my-2 my-md-0">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" id="userDropdown" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="fas fa-user-secret fa-fw"></i></a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
                {% if is_granted(constant('App\\Security\\Roles::LOGGED_IN')) %}
                    <a class="dropdown-item disabled">{{ app.user.username }}</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('app_logout') }}">{{ 'logout'|trans({}, 'menu') }}</a>
                {% else %}
                    <a class="dropdown-item" href="{{ path('app_login') }}">{{ 'login'|trans({}, 'menu') }}</a>
                {% endif %}
            </div>
        </li>
    </ul>
    <!-- Navbar-->
    </div>

</nav>

